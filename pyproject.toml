[project]
name = "aurex-corp-vtt"
version = "0.1.0"
description = "A local-first minimal virtual tabletop for Dungeons & Dragons."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }

[tool.pytest.ini_options]
testpaths = ["tests"]
